// Chapter 7.cpp : Defines the entry point for the console application.
// Programs and Topics of Chapter 7

#include "stdafx.h"
#include <iostream>
using namespace std;

/*
const int ROWS = 3;
const int COLUMNS = 5;
int total = 0;

void printScores(int [][COLUMNS], int);

int main()
{
	

	int scores[ROWS][COLUMNS] = { { 88, 97, 79, 86, 94 },
	{ 86, 91, 78, 79, 84 },
	{ 82, 73, 77, 82, 89 } };

	int newScores[ROWS][COLUMNS] = { {} };

	for (int i = 0; i < ROWS; i++)
	{
		for (int j = 0; j < COLUMNS; j++)
		{
			cout << scores[i][j] << "  ";
		}
		cout << endl;
	}
	
	for (int i = 0; i < ROWS; i++)
	{
		for (int j = 0; j < COLUMNS; j++)
		{
			cout << newScores[i][j] << "  ";
		}
		cout << endl;
	}
	

	printScores(scores, ROWS);

	cout << "The sum of each test is " << endl;
	for (int j = 0; j < COLUMNS; j++)
	{
		for (int i = 0; i < ROWS; i++)
		{
			total += scores[i][j];
		}
		cout << total << endl;
		total = 0;
	}

	system("pause");
    return 0;
}

void printScores(int myArr[][COLUMNS], int rows)
{
	for (int i = 0; i < rows; i++)
	{
		for (int j = 0; j < COLUMNS; j++)
		{
			cout << myArr[i][j] << "  ";
		}
		cout << endl;
	}
}
*/

/*
const string month[] = { "January", "February", "March", "April", "May", "June", "July",
"August", "September", "October", "November", "December"};

int main()
{
	auto array_length = end(month) - begin(month);	//find the number of elements in array

	for (int i = 0; i < array_length; i++)
	{
		cout << month[i] << ", ";
	}

	cout << endl;
	system("pause");
	return 0;
}
*/
